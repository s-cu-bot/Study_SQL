SELECT DATE_FORMAT(DATETIME, '%k') AS HOUR , COUNT(DATETIME) AS COUNT FROM ANIMAL_OUTS WHERE DATE_FORMAT(DATETIME, '%k') > 8 AND DATE_FORMAT(DATETIME, '%k') < 20 GROUP BY HOUR ORDER BY DATE_FORMAT(DATETIME, '%H') ASC
